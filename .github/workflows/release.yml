name: Release

on:
  push:
    branches:
      - main

jobs:
  tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      new_tag: ${{ steps.tag_version.outputs.new_tag }}
      new_version: ${{ steps.tag_version.outputs.new_version }}
      changelog: ${{ steps.tag_version.outputs.changelog }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create Tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    needs:
      - tag
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.tag.outputs.new_tag }}
          name: Release v${{ needs.tag.outputs.new_version }}
          body: ${{ needs.tag.outputs.changelog }}
          draft: false
          prerelease: false
          files: |
            ${{ env.PACKAGE }}
            CHANGELOG.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  ansible-galaxy-import:
    runs-on: ubuntu-latest
    needs: release
    permissions:
      contents: read
    steps:
      - name: Fail if ANSIBLE_GALAXY_TOKEN is not set
        run: |
          if [ -z "${{ secrets.ANSIBLE_GALAXY_TOKEN }}" ]; then
            echo "ANSIBLE_GALAXY_TOKEN secret is not set."
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Ansible
        run: |
          python -m pip install --upgrade pip
          pip install ansible

      - name: Import role to Ansible Galaxy
        env:
          ANSIBLE_GALAXY_TOKEN: ${{ secrets.ANSIBLE_GALAXY_TOKEN }}
        run: |
          ansible-galaxy role import --api-key "$ANSIBLE_GALAXY_TOKEN" rafikbahri ansible-role-glpi
